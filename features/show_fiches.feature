Feature: show fiches for a dci
  In order to distinguish fiches by validation
  As an auth user
  I want to choose between validated and non validated fiches

  Scenario: show link to render fiches alternating valide non valides

  Scenario: hide link to render fiches of different state if empty

  Scenario: don't render fiches if list is empty
