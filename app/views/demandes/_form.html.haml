= semantic_form_for @demande do |f|
  = f.error_messages
  - f.inputs do
    = f.input :nature,
      :label => "Nature de la demande",
      :as => :select,
      :collection => Demande::NATURES
    = f.input :name, :label => "Libellé à créer"
    = f.commit_button "Enregistrer"
