- title "Utilisateurs"

%table.table.table-striped
  - @users.each do |u|
    %tr
      %td= u.username
      %td= u.admin? ? "admin" : u.role
      %td= link_to "Modifier", edit_user_path(u)
      %td
        = link_to "Détruire", u,
          'data-confirm': t('confirm'),
          method: :delete if (can? :destroy, u) and ( current_user != u )

%p= link_to "Nouvel utilisateur", new_user_path, class: 'btn btn-primary' if can?(:create, User)
