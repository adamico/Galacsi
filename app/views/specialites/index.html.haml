- title "Liste des Spécialités"

%table
  - for specialite in @specialites
    %tr
      %td= link_to h(specialite.name), specialite
      %td= link_to 'Modifier', edit_specialite_path(specialite) if can? :edit, Specialite
      %td= link_to 'Détruire', specialite, :confirm => 'Are you sure?', :method => :delete if can? :delete, Specialite
