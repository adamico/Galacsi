- title "Rechercher un médicament"

= render "search_form"

- if params[:search]
  - if @dcis.empty?
    - search_result
  - else
    %table#list
      %thead
        %tr
          %th DCI
          %th Spécialité(s)
          %th Classe(s) Thérapeutique(s)
      = render @dcis
