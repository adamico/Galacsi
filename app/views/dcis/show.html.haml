-# encoding: utf-8
- title @dci.name.humanize

%p
  = link_to "Modifier cette dci", edit_dci_path(@dci) if permitted_to? :edit, @dci
  |
  = link_to "Détruire cette dci", @dci, :confirm => 'Etes-vous sûr ?', :method => :delete if permitted_to? :delete, @dci
  |
  = link_to "Retour à la liste", dcis_path

- if !@dci.fiches.blank?
  #fiches
    = render :partial => @dci.fiches
- else
  %p Pas de fiche d'évaluation pour cette dci.

%p
  = link_to "Nouvelle Fiche", new_dci_fiche_path(@dci) if permitted_to? :create, @dci.fiches

