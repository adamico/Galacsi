<tr>
  <td><%= link_to h(produit.name), produit %></td>
  <td><%= h produit.decision.name rescue "" %></td>
  <% if permitted_to? :update, :produits %>
    <td><%= h produit.state %></td>
  <% end %>
  <td><%= link_to "Modifier", edit_produit_path(produit) if permitted_to? :edit, produit %></td>
</tr>
