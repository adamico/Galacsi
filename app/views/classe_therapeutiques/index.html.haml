- title "Classes Thérapeutiques"

%table
  %tr
  - for classe_therapeutique in @classe_therapeutiques
    %tr
      %td= link_to h(classe_therapeutique.name), classe_therapeutique
      - if permitted_to? :update, :classe_therapeutiques
        %td= link_to 'Modifier', edit_classe_therapeutique_path(classe_therapeutique)
      - if permitted_to? :delete, :classe_therapeutiques
        %td= link_to 'Détruire', classe_therapeutique, :confirm => 'Are you sure?', :method => :delete

- if permitted_to? :create, :classe_therapeutiques
  %p= link_to "Nouvelle Classe Thérapeutique", new_classe_therapeutique_path
